export type DisplayCourseTag = {
  id: string;
  name: string;
  assign: boolean;
};

export type DisplayCreditTag = {
  id: string;
  name: string;
  credit: string;
};

export type DisplayIcalTag = {
  id: string;
  name: string;
  courseCount: number;
};
